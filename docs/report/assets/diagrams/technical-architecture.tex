\begin{tikzpicture}[
  font=\sffamily\scriptsize,
  component/.style={
    draw=ctline, rounded corners=2mm, thick, fill=white,
    minimum width=33mm, minimum height=12mm, align=center
  },
  core/.style={component, draw=ctgreen, fill=ctmint},
  protocol/.style={component, draw=ctblue, fill=blue!5},
  external/.style={component, dashed, draw=ctamber, fill=yellow!7},
  storage/.style={component, draw=ctblue, fill=purple!5},
  optional/.style={component, dashed, draw=ctgray, fill=gray!4},
  flow/.style={-{Latex[length=2.1mm]}, thick, draw=ctgray},
  bidir/.style={{Latex[length=2.1mm]}-{Latex[length=2.1mm]}, thick, draw=ctgray},
  label/.style={fill=white, inner sep=1.2pt, font=\sffamily\tiny}
]
  \node[external, minimum width=112mm] (services) at (0,7.2)
    {\textbf{Services applicatifs externes}\\
     Google OAuth 2.0 \quad|\quad Resend / SMTP \quad|\quad WireMock / futur PSP};

  \node[external] (osm) at (-4.2,4.6)
    {\textbf{OpenStreetMap}\\tuiles cartographiques};
  \node[component, fill=purple!7] (browser) at (0,4.6)
    {\textbf{Navigateur}\\React + TypeScript};
  \node[core] (reverb) at (4.2,4.6)
    {\textbf{Reverb}\\canaux privés};

  \node[core] (async) at (-4.2,1.7)
    {\textbf{Worker + Scheduler}\\jobs et tâches};
  \node[core] (api) at (0,1.7)
    {\textbf{API Laravel}\\REST, policies, services};
  \node[storage] (postgres) at (4.2,1.7)
    {\textbf{PostgreSQL}\\données, queue, cache};

  \node[protocol] (gateway) at (0,-1.3)
    {\textbf{Passerelle OCPP}\\Python + WebSocket};
  \node[optional] (redis) at (4.2,-1.3)
    {\textbf{Redis}\\optionnel};
  \node[external] (stations) at (0,-4.2)
    {\textbf{Bornes}\\physiques ou SAP};

  \draw[flow] (osm) -- (browser);
  \draw[bidir] (browser) -- node[label, left]{HTTPS / JSON} (api);
  \draw[bidir] (browser) -- node[label, above]{WebSocket} (reverb);
  \draw[flow] (api) -- (async);
  \draw[bidir] (api) -- (postgres);
  \draw[bidir] (async.south) -- ++(0,-0.45) -|
    node[label, near start, below]{jobs} (postgres.south);
  \draw[bidir] (api) -- node[label, left]{HTTP signé HMAC} (gateway);
  \draw[bidir] (gateway) -- node[label, left]{OCPP 1.6J} (stations);
  \draw[dashed, bidir] (postgres) -- (redis);
  \draw[bidir] (services.south west) to[out=-120,in=145] (api.north west);

\end{tikzpicture}
